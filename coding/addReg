global _start
_start:

        mov ax, [n1]
        mov bx, 6
        add bx, ax
        mov ax,bx
        mov bx, [n1]
        add bx, ax

        call xt

xt:
        mov ax, 1
        int 0x80

n1: dw 5
n2: dw 0
